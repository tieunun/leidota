#ifndef __UI_VIEW_ID_ENUM_H__
#define __UI_VIEW_ID_ENUM_H__

/**
* 每一个视图都有一个唯一标示，这里认为每一个视图当前最多只会存在一个 
*/
enum UIViewIdEnum
{
    UIVIEW_ID_INVALID,                          // 无效的id
    UIVIEW_ID_BATTLE                            // 战斗的时候的UI
};

#endif